<% if @answer.errors.any? %>
  $('.answer-errors').html('<%= render 'shared/errors', resource: @answer %>')
<% else %>
  $('.answers').append("<%= j render(partial: @answer) %>")
  $('textarea#answer_body').val('')
<% end %>

