h1 = @question.title
p = @question.body

= link_to 'Delete', question_path(@question), method: 'DELETE' if current_user&.author_of?(@question)

.answers
  h3 Answers:
  = render @question.answers

= render 'shared/errors', resource: @answer
= render 'questions/answer_form'
