require 'acceptance_helper'

feature 'Sign in via OAuth', %q{
  In order to interact with the system without filling my credentials
  As user
  I want to be able to sign in using OAuth
} do

  scenario 'user signs in via twitter' do
    pending "a specification" do
    end
  end
end